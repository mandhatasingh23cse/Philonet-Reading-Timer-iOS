# Read Later

iOS 17+ SwiftUI read-it-later application. Open `ReadLater.xcodeproj` with Xcode 16 or newer and select the **ReadLater** scheme.

The Share Extension writes incoming URLs to the shared App Group inbox. The host app imports that inbox into SwiftData whenever it becomes active.
